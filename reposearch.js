var repoform = document.getElementById('reposearch');

form.addEventListener('submit', function(e){
    e.preventDefault()

    var git_user = document.forms["reposearch"]["search"].value;
    var git_repo = document.forms["reposearch"][].value;
})